package com.study.study;

public class git2 {
    /*
    이클립스에서 깃 허브 등록 방법
    terminal
    git add .
    git commit -m""
    git push

    git 확인 하는법(사용자 확인)
    git bash 실행 git config--global user.name
                 git config--global user.email

    자격증명 관리자에서 git 계정 확인

    git 허브에서 불러오는법(pull)
    git bash 실행 git clone (주소복사) 파일명(내가만드는것)

    git 다른 컴퓨터에서 하는법(사용자 변경)
    자격증명관리 - Windows 자격증명 git 제거
    git bash 실행 git config--global user.name
                 git config--global user.email
                 git hub 로그인


     */
    
}
